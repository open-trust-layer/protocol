name: OLP conformance

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test-and-conformance:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install package and test dependencies
        run: python -m pip install -e '.[test]'

      - name: Run repository tests
        run: python -m pytest -q

      - name: Run OLP core conformance profile
        run: olp-conformance run --profile core-v1 --quiet --report conformance-report.json

      - name: Run OLP bundle-v1 conformance profile
        run: olp-conformance run --profile bundle-v1 --quiet --report conformance-bundle-report.json

      - name: Run OLP resolution-v1 conformance profile
        run: olp-conformance run --profile resolution-v1 --quiet --report conformance-resolution-report.json

      - name: Run OLP identity-authority-lifecycle-v1 conformance profile
        run: olp-conformance run --profile identity-authority-lifecycle-v1 --quiet --report conformance-identity-authority-lifecycle-report.json

      - name: Run OLP privacy-disclosure-v1 conformance profile
        run: olp-conformance run --profile privacy-disclosure-v1 --quiet --report conformance-privacy-disclosure-report.json

      - name: Assert Milestone 22 privacy disclosure conformance count
        run: |
          python - <<'PY'
          import json
          report = json.load(open("conformance-privacy-disclosure-report.json", encoding="utf-8"))
          results = report["results"]
          assert len(results) == 18, f"expected 18 privacy-disclosure-v1 cases, got {len(results)}"
          failed = [item for item in results if item["status"] != "PASS"]
          assert not failed, failed
          print("Python privacy-disclosure-v1 conformance: 18/18 PASS")
          PY

      - name: Upload conformance report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: conformance-python-${{ matrix.python-version }}
          path: |
            conformance-report.json
            conformance-bundle-report.json
            conformance-resolution-report.json
            conformance-identity-authority-lifecycle-report.json
            conformance-privacy-disclosure-report.json
          if-no-files-found: error

  rust-interoperability:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Set up Rust 1.85.0
        uses: dtolnay/rust-toolchain@1.85.0

      - name: Install OpenSSL development library
        run: |
          sudo apt-get update
          sudo apt-get install --yes libssl-dev

      - name: Install Python package and test dependencies
        run: python -m pip install -e '.[test]'

      - name: Test independent Rust implementation
        run: cargo test --locked --manifest-path implementations/rust/Cargo.toml

      - name: Build independent Rust conformance adapter
        run: cargo build --release --locked --manifest-path implementations/rust/Cargo.toml

      - name: Run Rust implementation against official core-v1 corpus
        run: |
          olp-conformance run \
            --profile core-v1 \
            --adapter subprocess \
            --adapter-command implementations/rust/target/release/olp-conformance-adapter \
            --quiet \
            --report conformance-rust.json

      - name: Run Rust implementation against bundle-v1 corpus
        run: |
          olp-conformance run \
            --profile bundle-v1 \
            --adapter subprocess \
            --adapter-command implementations/rust/target/release/olp-conformance-adapter \
            --quiet \
            --report conformance-rust-bundle.json

      - name: Run Rust implementation against resolution-v1 corpus
        run: |
          olp-conformance run \
            --profile resolution-v1 \
            --adapter subprocess \
            --adapter-command implementations/rust/target/release/olp-conformance-adapter \
            --quiet \
            --report conformance-rust-resolution.json

      - name: Run Rust implementation against identity-authority-lifecycle-v1 corpus
        run: |
          olp-conformance run \
            --profile identity-authority-lifecycle-v1 \
            --adapter subprocess \
            --adapter-command implementations/rust/target/release/olp-conformance-adapter \
            --quiet \
            --report conformance-rust-identity-authority-lifecycle.json

      - name: Run Rust implementation against privacy-disclosure-v1 corpus
        run: |
          olp-conformance run \
            --profile privacy-disclosure-v1 \
            --adapter subprocess \
            --adapter-command implementations/rust/target/release/olp-conformance-adapter \
            --quiet \
            --report conformance-rust-privacy-disclosure.json

      - name: Assert Milestone 22 privacy disclosure conformance count
        run: |
          python - <<'PY'
          import json
          report = json.load(open("conformance-rust-privacy-disclosure.json", encoding="utf-8"))
          results = report["results"]
          assert len(results) == 18, f"expected 18 privacy-disclosure-v1 cases, got {len(results)}"
          failed = [item for item in results if item["status"] != "PASS"]
          assert not failed, failed
          print("Rust privacy-disclosure-v1 conformance: 18/18 PASS")
          PY

      - name: Assert Milestone 21 identity authority lifecycle conformance count
        run: |
          python - <<'PY'
          import json
          report = json.load(open("conformance-rust-identity-authority-lifecycle.json", encoding="utf-8"))
          results = report["results"]
          assert len(results) == 18, f"expected 18 identity-authority-lifecycle-v1 cases, got {len(results)}"
          failed = [item for item in results if item["status"] != "PASS"]
          assert not failed, failed
          print("Rust identity-authority-lifecycle-v1 conformance: 18/18 PASS")
          PY

      - name: Assert Milestone 20 resolution conformance count
        run: |
          python - <<'PY'
          import json
          report = json.load(open("conformance-rust-resolution.json", encoding="utf-8"))
          results = report["results"]
          assert len(results) == 16, f"expected 16 resolution-v1 cases, got {len(results)}"
          failed = [item for item in results if item["status"] != "PASS"]
          assert not failed, failed
          print("Rust resolution-v1 conformance: 16/16 PASS")
          PY

      - name: Assert Milestone 19 bundle conformance count
        run: |
          python - <<'PY'
          import json
          report = json.load(open("conformance-rust-bundle.json", encoding="utf-8"))
          results = report["results"]
          assert len(results) == 8, f"expected 8 bundle-v1 cases, got {len(results)}"
          failed = [item for item in results if item["status"] != "PASS"]
          assert not failed, failed
          print("Rust bundle-v1 conformance: 8/8 PASS")
          PY

      - name: Assert Milestone 17 conformance count
        run: |
          python - <<'PY'
          import json
          report = json.load(open("conformance-rust.json", encoding="utf-8"))
          results = report["results"]
          assert len(results) == 62, f"expected 62 core-v1 cases, got {len(results)}"
          failed = [item for item in results if item["status"] != "PASS"]
          assert not failed, failed
          print("Rust core-v1 conformance: 62/62 PASS")
          PY

      - name: Run bidirectional Python-Rust interoperability tests
        env:
          OLP_RUN_RUST_INTEROP: "1"
        run: python -m pytest -q tests/interoperability/test_python_rust.py

      - name: Upload Rust conformance report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: conformance-rust-1.85.0
          path: |
            conformance-rust.json
            conformance-rust-bundle.json
            conformance-rust-resolution.json
            conformance-rust-identity-authority-lifecycle.json
            conformance-rust-privacy-disclosure.json
          if-no-files-found: error
